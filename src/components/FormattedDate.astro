---
import 'dayjs/locale/it'

import dayjs from 'dayjs'

interface Props {
    date: string
}

const { date } = Astro.props

const formatted = dayjs(date).locale('it').format('DD MMMM YYYY')
---

<time>
    {formatted}
</time>
