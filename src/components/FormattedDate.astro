---
interface Props {
    date: Date
}

const { date } = Astro.props
---

<time datetime={date.toISOString()}>
    {
        date.toLocaleDateString('it', {
            day: 'numeric',
            month: 'short',
            year: 'numeric',
        })
    }
</time>
